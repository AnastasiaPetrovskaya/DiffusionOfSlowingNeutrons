<Window x:Class="DiffusionOfSlowingNeutrons.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Diffusion of slowing neutrons" Height="500" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Name="btnStart"            
            Click="Button_Click"
            Grid.Row="0">
            Start shit
        </Button>
        <Label Name="lblPlotName"
               Grid.Row="0"
               Grid.Column="1"
               HorizontalAlignment="Center">
            Neutron position
        </Label>
        <Viewport3D Name="vpNeutrons"
                Grid.Row="1"
                Grid.Column="1"
                ClipToBounds = "False"
                IsHitTestVisible="false"
                RenderOptions.EdgeMode="Aliased"
                >
            <Viewport3D.Camera>
                <OrthographicCamera x:Name="camera" Width="2"
                    FarPlaneDistance="10"
	                NearPlaneDistance="1" 
	                LookDirection="0,0,-1"
	                UpDirection="0,1,0"
	                Position="0,0,2" />
            </Viewport3D.Camera>
            <Viewport3D.Children>
                <ModelVisual3D x:Name="Light1">
                    <ModelVisual3D.Content>
                        <DirectionalLight Color="White" Direction="1, 1, -1" />
                    </ModelVisual3D.Content>
                </ModelVisual3D>
                <ModelVisual3D x:Name="Light2">
                    <ModelVisual3D.Content>
                        <DirectionalLight Color="White" Direction="-1, 1, -1" />
                    </ModelVisual3D.Content>
                </ModelVisual3D>
                <ModelVisual3D x:Name="Light3">
                    <ModelVisual3D.Content>
                        <DirectionalLight Color="White" Direction="0,-1,-0.5" />
                    </ModelVisual3D.Content>
                </ModelVisual3D>
            </Viewport3D.Children>
        </Viewport3D>
        <Canvas x:Name="canvasOn3D"
                Grid.Column="1"
                Grid.Row="1"
                Background="Transparent" 
                MouseUp="vpNeutrons_MouseUp"
                MouseMove="vpNeutrons_MouseMove"
                MouseDown="vpNeutrons_MouseDown" Margin="0,0,0,0">
        </Canvas>
        <Label Name="lblInfo"
               Grid.Row="2"
               Grid.Column="1">
            Neutron: 0
            Step: 0
            Energy: 0
            Position: {0, 0, 0}
        </Label>
        <Button Name="btnNextStep"
                Grid.Row="2"
                Grid.Column="0">
            Next Step
        </Button>
    </Grid>
</Window>
